<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Create User</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="w-full h-screen bg-zinc-900 flex flex-col items-center justify-center text-white">
    <h3 class="text-3xl mb-6 text-blue-500 font-semibold">Create User</h3>

    <form method="POST" action="/login" class="flex flex-col w-1/3 gap-4">
      <!-- username -->
      
      <!-- email -->
      <input
        type="email"
        name="email"
        placeholder="Email"
        class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800 outline-none"
        required
      />
      <!-- password -->
      <!-- <input
        type="password"
        name="password"
        placeholder="Password"
        class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800 outline-none"
        required
      /> -->
      <!-- age -->
      
      <!-- submit -->
      <input
        type="submit"
        value="Login"
        class="px-3 py-2 bg-blue-500 border-2 border-zinc-800 rounded-md cursor-pointer hover:bg-blue-600"
      />
    </form>
  </div>
</body>
</html>
