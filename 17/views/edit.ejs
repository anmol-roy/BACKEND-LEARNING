<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>User Profile</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-800 min-h-screen flex flex-col items-center p-6">

  <!-- Header -->
  <div class="w-full flex justify-between items-center mb-8 max-w-3xl">
    <h1 class="text-2xl font-bold text-blue-600">User Profile</h1>
    <a href="/logout" class="bg-red-500 text-white px-4 py-2 rounded-md hover:bg-red-600 transition">Logout</a>
  </div>

  <!-- Profile Info -->
  <div class="bg-white shadow-md rounded-lg p-6 w-full max-w-3xl">
    <p class="text-gray-700 text-lg mb-2">Welcome to your edit  page!</p>
</div>

  <!-- Create Post -->
  <div class="bg-white shadow-md rounded-lg p-6 w-full max-w-3xl mt-6">
    <!-- edit page -->
    <h5 class="text-lg font-semibold mb-3">You can edit this post</h5>
    <form action="/update/<%= post._id %>" method="post" class="flex flex-col gap-4">
  <textarea
    class="w-full h-28 resize-none border border-gray-300 rounded-md p-3 focus:ring-2 focus:ring-blue-500 focus:outline-none"
    name="content"
  ><%= post.content %></textarea>
  
  <input
    value="Edit this Post"
    type="submit"
    class="bg-yellow-600 text-white font-semibold py-2 rounded-md hover:bg-blue-700 transition cursor-pointer"
  />
</form>

  </div>
</body>
</html>
